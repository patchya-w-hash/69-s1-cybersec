services:
  db:
    image: postgres:16-alpine
    container_name: 69-s1-db
    ports:
      
"54327:5432"
  environment:
POSTGRES_PASSWORD=xiy=Pk
POSTGRES_USER=patchya
POSTGRES_DB=patchya
volumes:
./data-postgres:/var/lib/postgresql/data

  admin:
    image: dpage/pgadmin4
    container_name: 69-s1-admin
    environment:
      
PGADMIN_DEFAULT_EMAIL=theerachut@test.com 
PGADMIN_DEFAULT_PASSWORD=pass2468
ports:
"8081:80"
depends_on:
db
volumes:
data-admin:/var/lib/pgadmin

volumes:
  data-admin:

